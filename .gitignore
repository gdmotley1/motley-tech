node_modules/
.DS_Store
*.log

# Local Claude Code config (not for public repo)
.claude/

# Internal lead lists / prospect notes (never publish)
leads/

# Prospect pitch sites — Grant decides per-prospect when to publish.
# Remove this line and re-stage the specific subfolder when ready.
prospects/
